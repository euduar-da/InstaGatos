spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/instagatos
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    show_sql: true
    properties:
      hibernate:
        format_sql: true

server:
  error:
    include-message: always

jwt:
  public:
    key: classpath:authz.pub
  private:
    key: classpath:authz.pem